apiVersion: v1
kind: Service
metadata:
  name: "service"
  namespace: $K8S_NAMESPACE
spec:
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: NodePort
  selector:
    app: $K8S_NAMESPACE